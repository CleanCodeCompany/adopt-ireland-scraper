const { validator } = require('../../src/lib/validator');

describe('lib --> validator', () => {
  it('should accpet properlly formatted list', () => {

  });
  it('should reject properlly formatted list', () => {

  });
});
